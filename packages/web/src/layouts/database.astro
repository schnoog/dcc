---
import Main from "./main.astro";

export interface Props {
	description: string;
	keywords: string;
}

const { description, keywords } = Astro.props;
---

<Main description={description} keywords={"Database, " + keywords}>
	<article class="w-full p-10">
		<slot />
	</article>
</Main>
