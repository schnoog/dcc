---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<div class="flex flex-col gap-4 rounded-md bg-midnightLight p-2">
	<h2 class="flex h-12 items-center border-b-2 border-b-gray-500 text-xl">{title}</h2>
	<ul class="flex flex-col gap-2">
		<slot />
	</ul>
</div>
